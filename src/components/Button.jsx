

function Button(props) {

    return (
        <div>
            <br />
            <button>{props.text}</button>
        </div>
    )
}

const array = []
const name = "Chelsea"

export default Button


export { array, name }


